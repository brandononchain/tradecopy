# SignalBridge — Environment Variables
# Only these 4 are needed in Railway (or any cloud host).
# All broker credentials are configured via the dashboard UI and stored in Supabase.

# ── Server ─────────────────────────────────────────────────────────────────────
PORT=3000
NODE_ENV=production

# ── Supabase (required — persistent storage for routes, connectors, settings) ──
SUPABASE_URL=https://qdbeeyqrquhrcvjeiazo.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InFkYmVleXFycXVocmN2amVpYXpvIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzQ5MDc1NDMsImV4cCI6MjA5MDQ4MzU0M30.RXaRjqHq-6dCu-pA-dph8wOCNSOQt75AQvaCTvP-eBY

# ── That's it. Everything else is configured in the dashboard: ─────────────────
# - Broker credentials  → Connectors page
# - Signal routes       → Signal Routes page
# - Symbol mappings     → Symbol Map page
# - Risk settings       → Settings page
